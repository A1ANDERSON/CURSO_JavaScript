/* "undefined" indica que no se ha inicializado una variable y que el valor esta ausente */
let indefinida; /* No le asignamos valor */
console.log(indefinida);

/* "null" es un valor especial que indica la ausencia de un valor */
let nulo = null; /* La variable esta vacia pero intencionalmente */
console.log(null);

/* NaN = Not a Number, cuando hacemos operaciones matematicas con algun dato que no es un numero */
let noEsUnNumero = "Hola" * 3.7;
console.log(noEsUnNumero);
